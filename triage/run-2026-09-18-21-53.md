
===== TRIAGE 2026-09-18 21:53:04 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead new-leads.txt
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead .
[0m→ [0mRead probe-results.md
[0m→ [0mRead probe-results.txt
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory
[0m→ [0mRead inventory/gamdom.md
[0m→ [0mRead triage-output.txt
[0m→ [0mRead triage-prompt.txt
[0m→ [0mRead probe-results.md[90m [offset=447][0m
[0m→ [0mRead inventory/gamdom.md[90m [offset=447][0m
Now let me read the rest of the inventory file and check the triage directory and the state files for the most recent lead summaries.
[0m→ [0mRead inventory/gamdom.md[90m [offset=854][0m
No leads were provided after the `LEADS:` marker in the prompt. However, I've read the full probe-results.md and inventory. Let me extract and triage the distinct findings the bot surfaced across all probe cycles.

---

## TRIAGE OUTPUT — Gamdom Program (bugs.olivermaicher.eu)

### Lead 1: tableau-admin.teamgamdom.com unauthenticated /api/3.21/serverInfo version disclosure

**Q1 In scope?** YES — `teamgamdom.com` is in scope; `tableau-admin.*` is company-owned infra.  
**Q2 Attacker reachable?** YES — public GET, no auth required.  
**Q3 Real security impact?** LOW — leaks productVersion 2025.1.11, build 20251.25.1210.1815, REST API 3.25, prepConductorVersion 2025.1.0. However, 2025.1.11 patches CVE-2025-52455 (SSRF) and CVE-2025-52449 (RCE). All other REST endpoints (/sites, /users, /datasources, etc.) return HTTP 401.  
**Q4 Provable non-invasively?** YES — single unauthenticated GET to `/api/3.21/serverInfo`.  
**Q5 Novel/unreported?** Unlikely — Tableau serverInfo endpoint is a documented default.  
**Q6 On always-rejected list?** YES — "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)" is explicitly out of scope.  
**Q7 Reasonable triager accept?** NO — informational version disclosure on a properly auth-gated admin service with patched CVEs.

**Verdict: INVALID** — Version disclosure only; all sensitive endpoints auth-gated; listed CVEs already patched in disclosed version.

---

### Lead 2: oauth2-proxy.teamgamdom.com shared CSRF cookie with Domain=teamgamdom.com

**Q1 In scope?** YES — `oauth2-proxy.teamgamdom.com` is company-owned infra on teamgamdom.com.  
**Q2 Attacker reachable?** YES — `/oauth2/start` is public and sets `_oauth2_proxy_csrf` cookie.  
**Q3 Real security impact?** LOW — Cookie has `Domain=teamgamdom.com; HttpOnly; Secure` set. The Domain attribute means the cookie is scoped to teamgamdom.com and all subdomains — this is standard oauth2-proxy behavior for shared cookie buckets. HttpOnly+Secure mitigates XSS theft. 302 redirect to Google OAuth with hardcoded `client_id=696342781525-...`.  
**Q4 Provable non-invasively?** YES — GET `/oauth2/start` returns Set-Cookie header.  
**Q5 Novel/unreported?** NO — This is default oauth2-proxy configuration, not a misconfiguration.  
**Q6 On always-rejected list?** Borderline — "Cookies that lack HTTP Only or Secure settings for non-sensitive data" is rejected, but this cookie HAS HttpOnly+Secure.  
**Q7 Reasonable triager accept?** NO — Standard oauth2-proxy behavior, properly secured cookie attributes.

**Verdict: INVALID** — Default oauth2-proxy configuration; cookie has HttpOnly+Secure; not a security finding.

---

### Lead 3: shreeram-dynamic-test.teamgamdom.com Basic realm="secret"

**Q1 In scope?** YES — `teamgamdom.com` subdomain.  
**Q2 Attacker reachable?** PARTIALLY — returns HTTP 401 Basic auth; realm string "secret" is disclosed.  
**Q3 Real security impact?** VERY LOW — The realm string "secret" is descriptive metadata, not a credential. The Basic auth gate holds across all 7 probed paths. This is an internal test backend properly gated.  
**Q4 Provable non-invasively?** YES — 401 response includes `WWW-Authenticate: Basic realm="secret"`.  
**Q5 Novel/unreported?** NO — Basic auth realm disclosure is well-known and informational.  
**Q6 On always-rejected list?** YES — "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)" explicitly covers realm string disclosure.  
**Q7 Reasonable triager accept?** NO — Trivial informational disclosure on a properly gated test backend.

**Verdict: INVALID** — Realm string disclosure on properly auth-gated internal test backend; out of scope per program policy.

---

### Lead 4: kargo.teamgamdom.com exposed ArgoCD-like API surface

**Q1 In scope?** YES — `teamgamdom.com` subdomain.  
**Q2 Attacker reachable?** YES — root returns HTTP 405, all `/api/v1/*` GET return 200 (SPA catch-all, Monaco IDE shell), POST return 405.  
**Q3 Real security impact?** NO — GET requests return SPA HTML (Monaco IDE catch-all), not API data. POST blocked with 405. Accept:application/json probe confirmed SPA ignores header. No JSON data surface exposed. Kargo v1.9.6 confirmed from client bundle.  
**Q4 Provable non-invasively?** YES — HTTP GET requests only.  
**Q5 Novel/unreported?** NO — SPA catch-all is standard deployment pattern.  
**Q6 On always-rejected list?** Effectively yes — no data exposure, just SPA shell.  
**Q7 Reasonable triager accept?** NO — No security impact; SPA serves frontend assets, API is method-gated.

**Verdict: INVALID** — SPA catch-all serves only frontend HTML; API endpoints properly method-gated (405 on POST); no data exposure.

---

### Lead 5: Perabet subdomain takeover — m.perabet.com dangling Fastly A-record

**Q1 In scope?** CONDITIONAL — perabet.com shares Gamdom's origin backend (byte-identical `/client-api`), so Gamdom operates it. However, `perabet.com` is not explicitly listed in scope.yml. Under "All company-owned infrastructure" policy, arguably in scope.  
**Q2 Attacker reachable?** YES — Varnish 500 "Fastly error: unknown domain: m.perabet.com" confirms dangling A-record. Claimable via attacker Fastly service.  
**Q3 Real security impact?** HIGH — Subdomain takeover allows attacker to serve content under a Gamdom-controlled brand domain, potentially phishing users of fatbets.com/perabet.com/gamdom.one ecosystem.  
**Q4 Provable non-invasively?** YES — GET request returns Varnish error confirming unclaimed domain.  
**Q5 Novel/unreported?** Possibly — 13+ consecutive cycles confirm persistent dangling state.  
**Q6 On always-rejected list?** NO — Subdomain takeover is not on the rejected list.  
**Q7 Reasonable triager accept?** MAYBE — Depends on whether perabet.com is considered in-scope brand. The shared origin backend confirms Gamdom operates it.

**Verdict: HOLD** — Potential subdomain takeover on a Gamdom-operated brand domain. Needs scope confirmation from program: is perabet.com in scope? If yes, this is a valid HIGH finding. Read-only proof: `GET https://m.perabet.com/` → Varnish 500 "unknown domain".

---

### Lead 6: Perabet VestaCP pre-auth API — tr.perabet.com:8083

**Q1 In scope?** CONDITIONAL — Same as Lead 5. `perabet.com` shares Gamdom's backend.  
**Q2 Attacker reachable?** YES — `/api/v1/login/` returns HTTP 200 JSON with guest token + full VestaCP config (VERSION=0.9.8, PHP=EOL, VESTA_CERTIFICATE=admin:giris.perabet.com). Full API route tree exposed (add/delete/bulk/list × user/web/db/dns/mail/firewall/cron/backup/IP/package/vesta). Auth-gated endpoints return 401.  
**Q3 Real security impact?** MEDIUM — Unauthenticated API surface with full endpoint enumeration; EOL PHP + EOL VestaCP 0.9.8; guest token issued pre-auth. However, all mutating endpoints are auth-gated (401).  
**Q4 Provable non-invasively?** YES — GET `/api/v1/login/` returns JSON.  
**Q5 Novel/unreported?** Possibly — VestaCP on a Gamdom brand is unexpected.  
**Q6 On always-rejected list?** NO — Pre-auth API surface and EOL software are not on the rejected list (but "Outdated software / library versions" IS explicitly rejected).  
**Q7 Reasonable triager accept?** UNCERTAIN — The EOL version disclosure is explicitly rejected. The pre-auth API surface is noteworthy but all sensitive endpoints require auth. The VESTA_CERTIFICATE leaking `admin:giris.perabet.com` is an informational credential hint but not exploitable without the key.

**Verdict: HOLD** — EOL software version is explicitly out of scope. Pre-auth API surface exists but is properly gated. Needs program clarification on whether VestaCP pre-auth API exposure on a shared-brand domain warrants a report. If program accepts: MEDIUM severity.

---

### Lead 7: Cross-brand shared origin backend (4 brands / 25+ hostnames)

**Q1 In scope?** YES — gamdom.com, fatbets.com, gamdom.one, perabet.com all share byte-identical `/client-api`.  
**Q2 Attacker reachable?** Partially — `/client-api` POST requires valid session cookie; GET returns 400 "only POST".  
**Q3 Real security impact?** THEORETICAL — The hypothesis is cross-brand session/cookie replay (same backend, same-origin cookies). However: (a) cookies are host-only (no Domain attribute), (b) `/client-api` OPTIONS returns no CORS headers, (c) auth transport = server-set same-origin cookie (no Bearer/localStorage), (d) cookie-domain-confusion is not observable pre-auth. This remains an unproven hypothesis at confidence 76.  
**Q4 Provable non-invasively?** NO — Requires authenticated session testing which would cross into active testing / credential usage.  
**Q5 Novel/unreported?** Possibly novel architecture concern.  
**Q6 On always-rejected list?** NO.  
**Q7 Reasonable triager accept?** NO — Unproven hypothesis; no concrete PoC; requires active auth testing.

**Verdict: INVALID** — Theoretical cross-brand ATO remains unproven. Auth transport (same-origin cookies, no CORS) makes passive replay infeasible. No concrete exploit path demonstrated.

---

### Lead 8: kargo.teamgamdom.com — Kargo v1.9.6 / direct origin IP (77.42.9.222)

**Q1 In scope?** YES — `teamgamdom.com` subdomain.  
**Q2 Attacker reachable?** YES — HTTP responses confirm service is live.  
**Q3 Real security impact?** LOW — Origin IP 77.42.9.222 with wildcard cert `*.teamgamdom.com` (Let's Encrypt, exp Oct 27). Direct origin bypasses Fastly CDN/WAF. However, API is SPA-gated with no JSON data exposure.  
**Q4 Provable non-invasively?** YES — DNS + HTTP GET.  
**Q5 Novel/unreported?** Origin IP disclosure via direct DNS is informational.  
**Q6 On always-rejected list?** Partially — "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)" covers informational disclosures.  
**Q7 Reasonable triager accept?** NO — Origin IP exposure is informational; no exploitable vulnerability demonstrated.

**Verdict: INVALID** — Origin IP + version info is informational only; API properly gated; no exploit path.

---

## Summary

| # | Lead | Verdict | Severity |
|---|------|---------|----------|
| 1 | Tableau serverInfo version disclosure | **INVALID** | Info |
| 2 | oauth2-proxy CSRF cookie Domain | **INVALID** | Info |
| 3 | shreeram Basic realm="secret" | **INVALID** | Info |
| 4 | kargo SPA catch-all | **INVALID** | None |
| 5 | m.perabet.com subdomain takeover | **HOLD** | HIGH (if in scope) |
| 6 | tr.perabet.com VestaCP pre-auth API | **HOLD** | MEDIUM (if in scope) |
| 7 | Cross-brand session sharing hypothesis | **INVALID** | Theoretical |
| 8 | kargo origin IP disclosure | **INVALID** | Info |

**Actionable holds:** Leads 5 and 6 require scope clarification from program on whether perabet.com (Gamdom-operated brand on shared backend) is in scope. If confirmed, Lead 5 (subdomain takeover) is the strongest candidate for a valid report.
