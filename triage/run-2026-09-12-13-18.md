
===== TRIAGE 2026-09-12 13:18:14 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
[0m✱ [0mGlob "**/*.md"[90m in . · 85 matches[0m
[0m✱ [0mGlob "**/*.yml"[90m in . · 1 match[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead new-leads.txt
[0m→ [0mRead probe-results.md
[0m→ [0mRead probe-results.txt
Your message ends with `LEADS:` but no lead content follows. Let me check the workspace for new leads to triage.
[0m→ [0mRead leads
No leads were provided after "LEADS:" in your message. Let me check the leads directory for the latest bot output.
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/reposcan-gamdom.md
[0m→ [0mRead triage/run-2026-09-12-09-51.md
[0m→ [0mRead leads/lead-nemotron3.md[90m [offset=340][0m
Now let me check the latest triage run for additional context, and also read the rest of the nemotron3 leads:
Now I have the full picture. Your message ended with `LEADS:` but nothing followed. I'll triage the distinct findings from the probe results (the most recent bot output) against the 7-Question Gate.

---

## TRIAGE — probe-results.md findings + lead hypotheses

---

### LEAD 1: `oauth2-proxy.teamgamdom.com` — Exposed OAuth2-Proxy with Google OAuth Client ID

**Q1 Scope:** Yes — `*.teamgamdom.com` is Gamdom-operated infrastructure (confirmed by other teamgamdom.com subdomains in use: kargo, tableau, staging).
**Q2 Reachable:** Yes — public, returns HTTP 401 (unauthenticated). Google OAuth redirect URL fully enumerable.
**Q3 Impact:** Medium — leaked Google OAuth `client_id` + `redirect_uri` enables OAuth phishing (user consents to attacker-controlled flow under `teamgamdom.com` origin); could also indicate misconfigured proxy if `/oauth2/auth` should not be public.
**Q4 Provable:** Yes — `GET https://accounts.google.com/o/oauth2/v2/auth?client_id=696342781525-...&redirect_uri=https://oauth2-proxy.teamgamdom.com/oauth2/callback` returns HTTP 200 (passive).
**Q5 Novel:** Likely — teamgamdom.com subdomain infrastructure not typically reported in bug bounty; OAuth client_id/redirect_uri leak is specific.
**Q6 Not always-rejected:** Yes — OAuth misconfiguration / client_id exposure is not on the always-rejected list.
**Q7 Reasonable triager:** Yes — an exposed OAuth2-Proxy with a Google OAuth flow on an internal-looking subdomain is a real finding.

**VERDICT: VALID**

**Proof steps:** `curl -s -o /dev/null -w "%{http_code}" "https://accounts.google.com/o/oauth2/v2/auth?client_id=696342781525-gf1piqd2ocgsgaiqifqbu1vc22khq227.apps.googleusercontent.com&redirect_uri=https://oauth2-proxy.teamgamdom.com/oauth2/callback&response_type=code&scope=profile+email"` → 200. Capture the full Google consent screen HTML.
**Impact:** OAuth phishing under trusted `teamgamdom.com` origin; potential session hijack if users complete the flow.
**CVSS 3.1:** AV:N/AC:L/PR:N/UI:R/S:U/C:L/I:L/A:N — **5.4 (Medium)**
**Channel:** bugs.olivermaicher.eu

---

### LEAD 2: `tableau-admin.teamgamdom.com/vib/api/` — Exposed Tableau Admin Panel

**Q1 Scope:** Yes — `*.teamgamdom.com` is Gamdom-operated.
**Q2 Reachable:** Yes — public, returns HTTP 401 (authentication gate present but panel exists).
**Q3 Impact:** Medium — admin analytics/BI panel existence leaks internal tooling; if auth is weak or default creds exist, full data access possible. "Exposure of auth data" rule only applies during testing, not reporting the existence.
**Q4 Provable:** Yes — `GET https://tableau-admin.teamgamdom.com/vib/api/` → 401 (passive).
**Q5 Novel:** Yes — Tableau admin panel on `teamgamdom.com` not commonly reported.
**Q6 Not always-rejected:** Yes — exposed admin panels with auth gates are not auto-rejected.
**Q7 Reasonable triager:** Yes — admin panel existence on a subdomain is a valid informational/medium finding.

**VERDICT: VALID**

**Proof steps:** `curl -s -o /dev/null -w "%{http_code}" https://tableau-admin.teamgamdom.com/vib/api/` → 401. `curl -s -I https://tableau-admin.teamgamdom.com/` → capture server header, check for Tableau version leak.
**Impact:** Internal BI tooling exposure; potential credential-stuffing target; data exfiltration if auth bypassed.
**CVSS 3.1:** AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N — **5.3 (Medium)**
**Channel:** bugs.olivermaicher.eu

---

### LEAD 3: `kargo.teamgamdom.com` — Kargo GitOps API Exposed

**Q1 Scope:** Yes — `*.teamgamdom.com` is Gamdom-operated.
**Q2 Reachable:** Yes — public, all endpoints return HTTP 405 (Method Not Allowed), confirming the API exists and accepts non-GET methods.
**Q3 Impact:** High — Kargo is a GitOps continuous promotion tool; exposed API could allow unauthorized deployment pipeline manipulation (applications, repositories, clusters, stages, freight).
**Q4 Provable:** Yes — `GET https://kargo.teamgamdom.com/api/v1/projects` → 405 (passive, confirms API surface).
**Q5 Novel:** Yes — Kargo instance exposure on Gamdom infrastructure is not commonly reported.
**Q6 Not always-rejected:** Yes — exposed deployment/CI tooling is not on the always-rejected list.
**Q7 Reasonable triager:** Yes — a public-facing GitOps API is a significant security concern.

**VERDICT: VALID**

**Proof steps:** `curl -s -o /dev/null -w "%{http_code}" https://kargo.teamgamdom.com/api/v1/projects` → 405. `curl -s https://kargo.teamgamdom.com/` → capture response body for version info. `curl -s -X POST https://kargo.teamgamdom.com/api/v1/projects -H "Content-Type: application/json" -d '{}'` → check for auth error vs data error.
**Impact:** Unauthorized access to deployment pipelines; code injection via GitOps; cluster compromise.
**CVSS 3.1:** AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N — **8.1 (High)**
**Channel:** bugs.olivermaicher.eu

---

### LEAD 4: `devsrv5.teamgamdom.com` — Dev Server Exposed

**Q1 Scope:** Yes — `*.teamgamdom.com` is Gamdom-operated.
**Q2 Reachable:** Yes — public, returns HTTP 400.
**Q3 Impact:** Low-Medium — dev server exposure may leak debug endpoints, stack traces, or internal APIs not meant for production.
**Q4 Provable:** Yes — `GET https://devsrv5.teamgamdom.com/` → 400 (passive).
**Q5 Novel:** Possibly — depends on what's behind the 400.
**Q6 Not always-rejected:** Yes — dev server exposure is not auto-rejected (descriptive error messages IS rejected, but the existence of the server itself is not).
**Q7 Reasonable triager:** Partially — a bare 400 with no further detail is weak. Needs path enumeration to confirm meaningful exposure.

**VERDICT: HOLD**

**Reason:** 400 alone is insufficient; needs path enumeration (e.g., `/api`, `/debug`, `/health`, `/admin`) to confirm meaningful information disclosure beyond "descriptive error messages" (always-rejected).
**Proof steps (if elevated):** Enumerate common paths on `devsrv5.teamgamdom.com` and check for non-400 responses or version/debug leaks.
**Impact:** Potential internal API/debug endpoint exposure — pending evidence.
**CVSS 3.1:** TBD — pending path enumeration.
**Channel:** bugs.olivermaicher.eu

---

### LEAD 5: `shreeram-dynamic-test.teamgamdom.com` / `staging.teamgamdom.com` — Test/Staging Subdomains

**Q1 Scope:** Yes — `*.teamgamdom.com` is Gamdom-operated.
**Q2 Reachable:** Both return HTTP 400 (public).
**Q3 Impact:** Low — test/staging environments may have weaker auth, debug features, or be running vulnerable software versions. However, 400 alone shows nothing.
**Q4 Provable:** Yes — passive HTTP status confirmed.
**Q5 Novel:** Marginal — staging exposure is common in bug bounty and often considered informational.
**Q6 Not always-rejected:** Yes — staging exposure is not on the always-rejected list, but "Outdated software / library versions" IS rejected. These are borderline.
**Q7 Reasonable triager:** No — bare 400 on test/staging subdomains with no further evidence of misconfiguration, data leak, or auth bypass is typically rejected as informational-only without a specific exploit path.

**VERDICT: INVALID**

**Reason:** Bare HTTP 400 on test/staging subdomains with no evidence of misconfiguration, data exposure, or authentication bypass. Classic "report from automated scanner without additional details or PoC" — explicitly rejected by scope.

---

### LEAD 6: Cross-Mirror Cookie Replay → ATO (from lead-bigpickle.md / lead-nemotron3.md)

**Q1 Scope:** Yes — gamdom80006.com, gamdom80007.com, gamdom4567.com, gamdom.com, gamdom.eu/io/vip/win are all in-scope.
**Q2 Reachable:** N/A — requires authenticated session (AUTH_HELPED).
**Q3 Impact:** High — cross-domain account takeover across all 8+ hostnames including flagship gamdom.com; single stolen cookie grants fleet-wide access to wallet/deposit flows.
**Q4 Provable:** Partially — origin trust boundary confirmed passively (byte-identical headers, same backend); but the critical proof (cookie replay) requires an authenticated session.
**Q5 Novel:** Yes — shared trust boundary across 7+ mirrors + flagship with host-agnostic cookie validation is a non-trivial architectural finding.
**Q6 Not always-rejected:** Yes — session fixation/cross-domain session issues are not on the always-rejected list.
**Q7 Reasonable triager:** Yes — if cookie replay is confirmed, this is a clear valid bug. Without it, the architectural characterization alone is strong recon but not yet a reportable vuln.

**VERDICT: HOLD**

**Reason:** Origin trust boundary confirmed (PASSIVE), but the actual exploit (cookie replay across hosts) requires AUTH_HELPED testing. Cannot report without the cookie replay proof. Queue for human-authorized session test.
**Proof steps (when ready):** Authenticate on gamdom80006.com, capture Set-Cookie headers (Domain/Path/SameSite attributes), replay exact Cookie header to gamdom80007.com/client-api and gamdom.com/client-api, compare response class vs invalid-cookie baseline.
**Impact:** Cross-domain ATO over wallet/deposit flows across entire Gamdom fleet — HIGH if confirmed.
**CVSS 3.1:** AV:N/AC:H/PR:N/UI:R/S:U/C:H/I:H/A:N — **6.8 (Medium) [pending confirmation → would be 8.1 High]**
**Channel:** bugs.olivermaicher.eu

---

### LEAD 7: `/client-api` Catch-All Proxy → SSRF / Mass-Assignment (from lead files)

**Q1 Scope:** Yes — gamdom.com/client-api and all mirrors are in-scope.
**Q2 Reachable:** Yes (GET returns 400; POST is the intended method).
**Q3 Impact:** Critical if confirmed — fund theft, ATO via upstream wallet/auth service.
**Q4 Provable:** No — requires POST with valid session to confirm; currently blocked by "no auth-bypass/mutate-against-live-data" learning.
**Q5 Novel:** Possibly — but entirely unproven.
**Q6 Not always-rejected:** Yes.
**Q7 Reasonable triager:** No — scanner output without PoC demonstrating a specific exploit is explicitly rejected by scope.

**VERDICT: INVALID**

**Reason:** No PoC, no evidence of actual SSRF or mass-assignment. Pure hypothesis from HTTP 400 status codes. Scope explicitly rejects "reports resulting from automated scanning utilities without additional details or a PoC demonstrating a specific exploit."

---

### LEAD 8: `tableau.teamgamdom.com` — Tableau Server (non-admin)

**Q1 Scope:** Yes — `*.teamgamdom.com`.
**Q2 Reachable:** Yes — returns HTTP 404 on various API paths.
**Q3 Impact:** Low — 404 responses show the server exists but no endpoints are reachable.
**Q4 Provable:** Yes — passive.
**Q5 Novel:** Marginal — already covered by Lead 2 (tableau-admin).
**Q6 Not always-rejected:** Borderline.
**Q7 Reasonable triager:** No — 404 on all probed paths is insufficient.

**VERDICT: INVALID**

**Reason:** All probed paths return 404; no accessible surface demonstrated. Subsumed by Lead 2 (tableau-admin).

---

## SUMMARY TABLE

| # | Lead | Verdict | CVSS | Key Reason |
|---|------|---------|------|------------|
| 1 | oauth2-proxy.teamgamdom.com Google OAuth client_id leak | **VALID** | 5.4 Med | Exposed OAuth flow under trusted origin |
| 2 | tableau-admin.teamgamdom.com admin panel | **VALID** | 5.3 Med | Admin analytics panel existence + auth gate |
| 3 | kargo.teamgamdom.com GitOps API | **VALID** | 8.1 High | Public deployment pipeline API (405 confirms surface) |
| 4 | devsrv5.teamgamdom.com | **HOLD** | TBD | 400 alone insufficient; needs path enumeration |
| 5 | shreeram-dynamic-test / staging | **INVALID** | — | Bare 400, no PoC, scanner-only |
| 6 | Cross-mirror cookie replay ATO | **HOLD** | 6.8→8.1 | Origin boundary confirmed, exploit needs AUTH_HELPED |
| 7 | /client-api SSRF/mass-assignment | **INVALID** | — | Pure hypothesis, no PoC, scanner-only |
| 8 | tableau.teamgamdom.com (non-admin) | **INVALID** | — | 404 on all paths, no surface |
