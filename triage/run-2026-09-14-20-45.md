
===== TRIAGE 2026-09-14 20:45:01 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/scope.yml"[90m 1 match[0m
[0m✱ [0mGlob "**/gamdom.md"[90m 1 match[0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m✱ [0mGlob "**/*probe*"[90m 2 matches[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/gamdom.md
[0m→ [0mRead probe-results.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead new-leads.txt
Now let me read the individual lead files to get the actual findings to triage:
[0m→ [0mRead leads/lead-bigpickle.md[90m [offset=438][0m
[0m→ [0mRead leads/lead-nemotron3.md[90m [offset=340][0m
Let me read the later parts of the lead files to capture the most recent findings:
[0m
[0m# [0mTodos
[•] Triage all distinct lead hypotheses from hunt bot output against 7-Question Gate
[ ] Output triage verdict with proof steps, impact, CVSS, reporting channel for VALID leads
[0m
Now I have enough data to triage. Let me extract the distinct lead hypotheses and apply the 7-Question Gate.
[0m# [0mTodos
[✓] Triage all distinct lead hypotheses from hunt bot output against 7-Question Gate
[✓] Output triage verdict with proof steps, impact, CVSS, reporting channel for VALID leads
[0m
Based on thorough analysis of all 6 model lead files, the probe results, and the scope/inventory, here is my triage:

---

## TRIAGE REPORT — Gamdom Hunt Bot Leads

### LEAD 1: Cross-mirror session cookie replay ATO via shared `/client-api` origin
**Verdict: HOLD**

| Gate | Answer |
|---|---|
| Q1 Scope | YES — gamdom80006.com, gamdom4567.com, gamdom.com + 4 regional TLDs + 7 mirrors are all in-scope (scope.yml: "All company-owned infrastructure, gaming engines, wallets, APIs, user accounts, web/mobile applications") |
| Q2 Reachable | YES — public, unauthenticated GET on all 14+ hosts confirmed |
| Q3 Impact | HIGH — if session cookie Domain is `.gamdom.com` or host-agnostic at the shared Starlette origin, single cookie theft = full account takeover across flagship + 4 TLDs + 7 mirrors (wallet, deposits, KYC) |
| Q4 Provable | **NOT YET** — all passive paths exhausted; needs AUTH_HELPED session to extract Set-Cookie Domain/Path/SameSite from authenticated `/client-api` POST, then replay on a different host |
| Q5 Novel | Partially — shared-origin architecture itself is a design concern, but no public report exists proving the cookie is host-agnostic; novelty hinges on the single Set-Cookie replay test |
| Q6 Not always-rejected | YES — cross-domain session fixation/replay via shared backend is not on the always-rejected list |
| Q7 Reasonable triager | **HOLD** — evidence is compelling (byte-identical /client-api, verified origin, cookie transport proven, CORS absent blocks client-side vector) but the one critical proof (Set-Cookie Domain attribute from authenticated POST) is missing. Would be accepted if that proof is provided |

**Reason:** Strong hypothesis with 10+ passive confirmations of shared origin trust boundary, but the sole remaining link in the exploit chain (host-agnostic session cookie at the Starlette backend) requires one authenticated request to confirm. Cannot accept without PoC; cannot reject because the architecture strongly implies the vulnerability exists.

**If VALID after auth test:** Report via bugs.olivermaicher.eu with session replay PoC (two browser contexts, different hostnames, same cookie accepted). CVSS 3.1: **AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N — 7.4 (High)**.

---

### LEAD 2: Brand-wide origin trust pool (gamdom.com + 4 TLDs + mirrors share one backend)
**Verdict: HOLD** (same gating as Lead 1 — this is the same hypothesis at fleet scope)

---

### LEAD 3: gamdom4567.com confirmed shadow origin
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | NO — this is infrastructure characterization (proven by passive header fingerprinting), not a standalone vulnerability. Knowing the origin exists doesn't grant access. |
| Q7 Reasonable triager | NO — triagers accept architecture findings only if they lead to an actionable exploit. This is a prerequisite finding, not a reportable vuln. |

**Reason:** Confirmed origin backend via byte-identical headers and Fastly 421 cert binding — solid recon but zero security impact on its own. Subsumed by Lead 1.

---

### LEAD 4: Uptime Kuma socket.io admin exposure (gamdommirrors.com)
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | NO — gamdommirrors.com is a self-hosted Uptime Kuma status page (third-party monitoring SaaS), not Gamdom's casino infrastructure. Defacing it has brand/trust impact but no wallet/auth data access. Out-of-scope reminder: "no exposure of customer/employee/financial/auth data during testing" applies to third-party SaaS too. |
| Q4 Provable | NO — socket.io issues SID but Fastly load-balancing breaks session persistence; admin events require POST RPC which is mutation. |
| Q7 Reasonable triager | NO — confidence 38 (below 40 threshold), third-party SaaS, POST required, no confirmed unauthenticated mutation surface. |

**Reason:** Self-hosted Uptime Kuma on Fastly with broken session persistence. No unauthenticated admin surface found after 10+ probe cycles. Always-rejected adjacent: "CSRF on logout" / "login panels" territory.

---

### LEAD 5: /client-api catch-all proxy SSRF/mass-assignment
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | CRITICAL *if proven* — but currently unproven |
| Q4 Provable | NO — requires authenticated POST with valid session to test action routing against upstream wallet/auth. Bundle action strings are obfuscated, no endpoint map extractable passively. |
| Q7 Reasonable triager | NO — confidence 48, verify_steps requires AUTH_HELPED live POST, and the REJECTED auth-bypass learning explicitly blocks blind POST against the live identity/wallet proxy. Scanner output alone is REJECTED per scope. |

**Reason:** Speculative hypothesis. No POST sent, no upstream error/stack/behavior differential observed. Cannot report "what if" as a vulnerability.

---

### LEAD 6: staffRefillConfig client-side leak (30M/75M coins)
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | NO — client-side config values in a JavaScript bundle are visible to every user by design. This is not "exposure of auth/financial data" — it's frontend constants. No privilege escalation path exists without server-side validation bypass (see Lead 5). |
| Q6 Not always-rejected | Borderline — falls under "Descriptive error messages or headers" / "Disclosure of known public files" — the JS bundle is served to every client. |
| Q7 Reasonable triager | NO — triagers reject client-side config leaks unless they enable a concrete exploit. No exploit chain demonstrated. |

**Reason:** Staff refill thresholds in client-side JS are normal frontend configuration, not a vulnerability. Would only matter if privilege were client-asserted (see Lead 5, which is also unproven).

---

### LEAD 7: click.gamdom.com third-party subdomain takeover
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | NO *today* — CNAME resolves to SymplifyMail (192.165.55.11) which serves live content. Not dangling. |
| Q4 Provable | NO — takeover is conditional on third-party provider lapsing in the future. Cannot prove a hypothetical. |
| Q7 Reasonable triager | NO — confidence 25, "not dangling today" = not a finding. Always-rejected adjacent: "Attacks dependent upon social engineering of employees or vendors" / "Use of known-vulnerable library" (conditional). |

**Reason:** Subdomain points to live third-party email provider. Would only be reportable if the CNAME target returned NXDOMAIN/404 with no owner. Currently benign.

---

### LEAD 8: dashboard.gamdom.com edge 403
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | NO — fully 403-locked at edge (Varnish Error 54113) across all paths. No bypass found. |
| Q4 Provable | NO — no non-403 response observed on any path/host variant. |
| Q7 Reasonable triager | NO — "publicly accessible login panels" and "descriptive error messages" are always-rejected. A 403-locked admin vhost is inventory knowledge, not a vulnerability. |

**Reason:** Access-controlled admin hostname on Fastly. No bypass, no information leakage. Standard infrastructure.

---

### LEAD 9: kargo.teamgamdom.com ArgoCD API (405 on POST)
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | NO — all tested endpoints (`/api/v1/projects`, `/applications`, `/repositories`, `/clusters`, `/stages`, `/freight`, `/version`) return HTTP 405 (Method Not Allowed). SPA catch-all serves 200 on GET. No unauthenticated data leak, no mutation possible. |
| Q4 Provable | Partially — 405 proves the API exists but doesn't demonstrate access. No 200 JSON response with data observed. |
| Q7 Reasonable triager | NO — 405 on all POST routes = properly gated. No data exposed. Would be rejected as "scanner output without PoC". |

**Reason:** ArgoCD-like API on internal tooling returns 405 on all mutation paths. SPA catch-all absorbs GET routes. No data leak, no auth bypass demonstrated.

---

### LEAD 10: tableau-admin.teamgamdom.com Tableau REST API
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | LOW — `/api/3.21/serverInfo` returns 200 (Tableau Server info), but all data endpoints (`/sites`, `/users`, `/workbooks`, `/datasources`, `/groups`, `/tasks`, `/schedules`, `/auth/signin`) return 401 XML "No authentication credentials were provided". |
| Q4 Provable | Partially — serverInfo endpoint is accessible, but server info (version, edition) is not sensitive customer/financial/auth data. |
| Q6 Not always-rejected | Partially — "Publicly accessible login panels" / "SSL/TLS best practices" territory. Tableau login panel visible at root (200 len=1989). |
| Q7 Reasonable triager | NO — Tableau REST API requires auth on all data endpoints. serverInfo returning version info is standard Tableau behavior. Login panel visibility is always-rejected. |

**Reason:** Internal Tableau instance with standard auth gating. Server version disclosure is low-impact and common across Tableau deployments. All sensitive endpoints require authentication.

---

### LEAD 11: oauth2-proxy.teamgamdom.com Google OAuth client_id exposure
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | NO — OAuth2 client_id values are public by design (they appear in browser URL bars during OAuth flows). The client_id `696342781525-gf1piqd2ocgsgaiqifqbu1vc22khq227.apps.googleusercontent.com` is visible to any user who initiates the Google sign-in flow. |
| Q6 Not always-rejected | YES — "Publicly accessible login panels" / "Lack of email address verification" territory. OAuth client_id exposure is standard. |
| Q7 Reasonable triager | NO — client_id is not a secret. client_secret is the secret, and it's server-side only. This is expected OAuth2 behavior. |

**Reason:** Google OAuth client_id is public information by OAuth2 spec design. The 401 on `/oauth2/auth` confirms proper auth gating.

---

### LEAD 12: grafana/prometheus/vault/clickhouse.teamgamdom.com (200 on root)
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q1 Scope | Gray area — `*.teamgamdom.com` is a brand subdomain, and the scope says "All company-owned infrastructure". However, these are internal DevOps tools behind OAuth2-proxy (confirmed by OAuth2-proxy teamgamdom.com returning 401). |
| Q3 Impact | NO — 200 on root for Grafana/Prometheus/Vault/ClickHouse is standard (login page or health check). These services are behind the oauth2-proxy (401 observed). No unauthenticated data access demonstrated. |
| Q4 Provable | NO — 200 responses are login pages/health endpoints, not data leaks. |
| Q7 Reasonable triager | NO — internal services behind OAuth2-proxy, no auth bypass, no data leak. Would be rejected as "infrastructure behind auth = not a vuln". |

**Reason:** Internal DevOps dashboards behind oauth2-proxy. Standard login/health endpoints returning 200 is expected behavior. No unauthenticated access to sensitive data.

---

### LEAD 13: devsrv5.teamgamdom.com / stagsrv.teamgamdom.com (Basic auth 401)
**Verdict: INVALID**

| Gate | Answer |
|---|---|
| Q3 Impact | NO — both return 401 (Basic auth required) on all paths (/, /env, /.git/config, /actuator, /server-status, /_metrics, /status). Properly access-controlled. |
| Q4 Provable | NO — no bypass, no data leak. 401 = auth required. |
| Q6 Not always-rejected | YES — "Publicly accessible login panels" is explicitly always-rejected. |
| Q7 Reasonable triager | NO — properly gated internal dev/staging servers. Basic auth working as intended. |

**Reason:** Development/staging servers with Basic auth returning 401 on all paths. Auth gate functioning correctly. No bypass, no information leakage.

---

## SUMMARY

| Lead | Verdict | Reason |
|---|---|---|
| Cross-mirror session cookie replay ATO | **HOLD** | Strong hypothesis, 10+ passive confirmations, but needs 1 authenticated Set-Cookie test to confirm host-agnostic cookie scope |
| Brand-wide origin trust pool | **HOLD** | Same gating as above |
| gamdom4567.com shadow origin | INVALID | Infrastructure characterization, no standalone vuln |
| Uptime Kuma socket.io admin | INVALID | Third-party SaaS, Fastly breaks persistence, POST required, conf 38 |
| /client-api SSRF/mass-assignment | INVALID | Speculative, requires authenticated POST, REJECTED class |
| staffRefillConfig client-side leak | INVALID | Client-side config values, not a vulnerability |
| click.gamdom.com takeover | INVALID | Not dangling today, conditional on future lapse |
| dashboard.gamdom.com 403 | INVALID | Fully access-controlled, no bypass |
| kargo.teamgamdom.com ArgoCD | INVALID | 405 on all mutation paths, no data leak |
| tableau-admin.teamgamdom.com | INVALID | Auth required on all data endpoints, serverInfo is benign |
| oauth2-proxy Google client_id | INVALID | Public by OAuth2 spec design |
| grafana/prometheus/vault/clickhouse | INVALID | Behind oauth2-proxy, 200 = login/health |
| devsrv5/stagsrv Basic auth | INVALID | Properly gated, 401 on all paths |

**Bottom line:** Zero reportable vulnerabilities found. One high-potential lead (cross-mirror session cookie replay ATO) is gated on a single authenticated request. All other leads are either out-of-scope, below threshold, speculative, or demonstrate normal service behavior.
**Recommended next action:** Execute the single AUTH_HELPED probe — authenticate on gamdom80006.com, extract Set-Cookie Domain/Path/SameSite from the authenticated `/client-api` response, then replay the cookie on gamdom.com/client-api (Pool A) and gamdom80007.com/client-api (Pool B). If the cookie is accepted cross-host, promote to VALID and report via bugs.olivermaicher.eu with the replay PoC.
